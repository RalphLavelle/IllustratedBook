The "slug' property on the book model and database entity should not exists. Slugs are only generated on the fly and are not persisted in the database. Remove all occurrences of Slug from migrations, database tables, etc.