Change the way the bookService looks up chapters and pages. Instead of trying to get them from the database, use the book id to get a JSON file from the Books folder and use that to find chapters and pages. Chapters and pages should not be persisted in the database.