Add a column to the Images table, of type NVARCHAR(MAX), called 'Metadata'. Remove these columns: ImageUrl, Model, ModelVersion, Width, Height, InferenceSteps, GuidanceScale, NegativePrompt, GeneratedAt, UpdatedAt.